{
  "name": "@nejcm/dev-toolbar-playground",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Unpublished manual-verification app for @nejcm/dev-toolbar. Not part of the package's `files`.",
  "scripts": {
    "predev": "npm --prefix ../.. run build",
    "dev": "vite",
    "prebuild": "npm --prefix ../.. run build",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@nejcm/dev-toolbar": "file:../..",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.7.0",
    "vite": "^6.0.7"
  }
}
