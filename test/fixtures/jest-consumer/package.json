{
  "name": "dtb-jest-consumer-fixture",
  "private": true,
  "version": "0.0.0",
  "description": "CommonJS/Jest consumer fixture for @nejcm/dev-toolbar. Not published; see README.md in this directory.",
  "scripts": {
    "test": "node sync-package.mjs && node ../../../node_modules/typescript/bin/tsc -p tsconfig.json && jest"
  },
  "devDependencies": {
    "@testing-library/react": "^16.3.3",
    "jest": "^30.5.1",
    "jest-environment-jsdom": "^30.5.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  },
  "engines": {
    "node": ">=24"
  }
}
